This repo is a companion simulation to a paper that extends the school choice model by including routes into preference lists.

The simulation uses real data found in "C:\Users\aejp1u19\geo-model\data."

NEVER GUESS, NEVER INVENT DATA, NEVER SUPPRESS ERRORS. If anything breaks or doesn't make sense, report it.

Package manager is uv.